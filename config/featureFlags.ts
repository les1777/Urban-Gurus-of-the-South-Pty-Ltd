// In a typical bundled React app, you would use environment variables
// like `process.env.REACT_APP_POSTPARTUM_MODULE_ENABLED === 'true'`.
// In this environment, we'll enable it by default to demonstrate the feature,
// as `process.env` is not available.
export const POSTPARTUM_MODULE_ENABLED = true;
